apiVersion: v1
kind: Service
metadata:
  name: foodhub-service
  labels:
    app: foodhub-app
spec:
  type: LoadBalancer
  selector:
    app: foodhub-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
